{
  "name": "dark-factory-contracts",
  "version": "0.0.1",
  "description": "",
  "author": "icerdesign (liangshuang@sutu.tech)",
  "license": "MIT",
  "scripts": {
    "build": "forge build --extra-output-files abi && typechain --target ethers-v6 --out-dir ../ui/src/contracts ../out/**/*.abi.json",
    "chain": "anvil --mnemonic-seed-unsafe 100",
    "deploy": "bash ./scripts/deploy.sh"
  },
  "devDependencies": {
    "@typechain/ethers-v6": "^0.4.0",
    "typechain": "^8.2.0"
  }
}